import {main} from './main.js';

let mainStyle = {
    display: 'flex',
    flexFlow: 'column wrap',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
    width: '100%'
}

let container = main.init(mainStyle);